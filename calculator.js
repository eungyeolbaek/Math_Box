// (x: number, y:number) -> number
function add(a, b) {
    return a + b;
}

// (x: number, y:number) -> number
function substract(a, b) {
    return a - b;
}

// (x: number, y:number) -> number
function multiply(a, b) {
    return a * b;
}